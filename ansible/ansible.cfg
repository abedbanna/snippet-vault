# The SSH private key is deliberately not set here. Provide it either with the
# ANSIBLE_PRIVATE_KEY_FILE environment variable or with
# `ansible-playbook --private-key ~/.ssh/id_ed25519 ...`.
[defaults]
inventory = ./inventory.ini
remote_user = root
host_key_checking = False
interpreter_python = auto_silent
