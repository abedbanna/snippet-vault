# Copy to .env (gitignored) and fill in values for your machine.

# TCP port the API listens on (default: 3000)
PORT=

# Exact browser origin allowed by CORS, e.g. the Vite dev server (default: http://localhost:5173). "*" is rejected.
CORS_ORIGIN=
